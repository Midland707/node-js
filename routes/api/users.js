const express = require("express");
const router = express.Router();

// router.post("/register", contactsController.getListContacts);
// router.post("/login", contactsController.getListContacts);
// router.post("/logout", contactsController.getListContacts);
// router.post("/current", contactsController.getListContacts);

module.exports = router;
